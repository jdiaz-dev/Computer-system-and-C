/* 
    CHAPTER SIX: Working with functions
        **Defining a Function
            --la primera línea de una función le dice al compilador cuatro cosas:
                1. quién puede llamar a este
                2. el tipo de valor que este retorna
                3. su nombre
                4. los argumentos que este toma
            
            --la palabra función y rutina son usados intercambiablemente, significan la misma cosa

        **Arguments and Local Variables
        **Function Prototype Declaration
        **Automatic Local Variables
        **Returning Function Results
        **Functions Calling Functions Calling...
        **Declaring Return Types and Argument Types
        **Checking Function Arguments
        **Top-Down Programming
            --las funciones que llaman funciones y estas a otras funciones, son la base para producir buenos programas estructurados

        **Functions and Arrays
            --La metodología de llamadas que llaman a otras funciones hace que los programas sean fáciles de escribir, leer y mantener

        **Assignment Operators
        **Sorting Arrays
        **Multidimensional Arrays
            //en el array multidimensional se puede omitir el número de filas pero debe ir obligatoriamente el número de filas

        **Multidimensional Variable-Length Arrays and Functions
        **Global Variables

        **Automatic and Static Variables
            --Una variable es automatica por defecto, asi que no es necesario anteponer la palabra clave "auto"
            --Una variable automática es creada cada vez que la función es llamada, tan pronto como ejecución de la función termine, esas variables desaparecen
            --Este proceso sucede automáticamente, de ahí el nombre variables automáticas
            --En una variable automática garantizado que no existiran la siguiente vez que se haga un llamado a la función
            
            --En una variable estática, dicha variable tendrá el mismo valor cuando se haga la siguiente llamada
            --Una variable estática es inicializado una vez en todo el programa y no cada vez que la función sea llamada
            --En las variables staticas su valor por defecto es 0, diferente a las variables autmáticas donde no tienen valores iniciales por defecto
            --La desición de usar una variable estática o automática depende del uso destinado
            --Por ejemplo para contar el numero de veces que se ha llamado a una función se puede usar una variable local

        **Recursive Functions
    
        
*/