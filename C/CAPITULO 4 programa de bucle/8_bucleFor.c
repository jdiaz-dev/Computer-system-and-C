#include <stdio.h>

int main(void)
{
    int contador = 0;

    //se puede hacer lo mismo con el bucle "for" y "while"
    while (contador <= 5){
        
        printf("%i\n", contador);
        contador++;
    }
    
}